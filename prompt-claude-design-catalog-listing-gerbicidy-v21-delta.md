# Delta-промпт: catalog-listing-gerbicidy.dc.html v21 → v22

**Проект:** редизайн сайта ООО «Восток АгроХим»
**Целевой файл:** `catalog-listing-gerbicidy.dc.html`
**Отсчётная версия:** v21 (Claude Design, выгрузка `Восток-Агрохим_Hero_блок__21_.zip`)
**Целевая версия:** v22
**Источник спеки:** `catalog-ia-v1.md` v1.4 §5, §8
**Дата:** 09.08.2026

---

## Контекст

Приёмка v21 против чек-листа delta v20→v21: 13 из 14 пунктов реализованы полностью. Три точечные правки на v22:

1. Отсутствует обязательный фильтр «Цена».
2. В фильтре «Страна производства» некорректное значение — Германия.
3. В блоке «Другие категории» неверный счётчик у Десикантов.

---

## 1. Добавить фильтр «Цена, ₽»

**Что не так в v21:** в `const GROUPS` отсутствует фильтр диапазона цен. По `catalog-ia-v1.md` v1.4 §8 — универсальный фильтр #17 (диапазон От–До + слайдер).

**Что должно стать:**

Добавить в массив `GROUPS` (в блок `extra: true`) элемент типа `range`:

```js
{
  key: "price",
  title: "Цена, ₽",
  type: "range",
  extra: true,
  min: 500,
  max: 12000,
  step: 100,
  unit: "₽/л"
}
```

**Рендер фильтра:**

- Два поля ввода: «От» и «До» — с плейсхолдерами со значениями `min` и `max`.
- Слайдер (двухточечный, dual-thumb range slider) — под полями ввода, с теми же границами.
- Тонкая связь: изменение слайдера обновляет поля ввода и наоборот.
- При сбросе (кнопка «Сбросить все» / крестик на активном чипе) — возвращаются граничные значения.
- Активный чип показывается только если диапазон уже, чем `[min; max]`. Формат чипа: «Цена: 1 200 – 5 800 ₽» (или «Цена: от 1 200 ₽», «Цена: до 5 800 ₽» — если ограничено с одной стороны).

**Замечание к реализации:**
Значения `min` / `max` в мокапе (500 / 12 000 ₽) — плейсхолдер. В финальной сборке (Phase 5) диапазон вычисляется динамически по текущей выборке — от минимальной до максимальной цены отфильтрованных препаратов.

Единицы измерения (`₽/л` vs `₽/кг`) — препарат к препарату разные (жидкости и порошки), поэтому в фильтре и в чипе показываем без единицы, просто «₽». Уточнение по единицам — при показе в карточке товара.

---

## 2. Фильтр «Страна производства» — убрать Германию

**Что не так в v21:**
```js
{ key: "country", title: "Страна производства", type: "multi", extra: true,
  items: [["Россия",38],["Китай",4],["Германия",2]] }
```

Значение «Германия» появилось без основания. В таблице верификации от заказчика (`Восток-АгроХим_каталог_верификация_v2.xlsx`, 92/92 позиций) все 6 регистрантов — российские юрлица. Карточек БАСФ / Байер / ФМРус / Листерра в фактическом ассортименте нет — их упоминание в блоке «Производители» на главной (ПИ07) — маркетинговое позиционирование, а не список товаров, из которого берётся значение поля «Страна производства».

**Что должно стать:**

```js
// Стартовый словарь по catalog-ia-v1.md v1.4 §5.
// Финальные значения — при миграции контента (Phase 4), по этикеткам производителей.
{ key: "country", title: "Страна производства", type: "multi", extra: true,
  items: [["Россия",38],["Китай",4],["Венгрия",2]] }
```

**Изменения:**
- «Германия» → «Венгрия» (по стартовому словарю §5)
- Числа для Китая (4) и Венгрии (2) оставляем как есть — плейсхолдер до миграции реальных данных.
- Комментарий над строкой — обязателен: явно указывает, что итоговые значения появятся в Phase 4.

**Почему Венгрия:** в стартовом словаре §5 catalog-ia-v1.md перечислены {Россия, Китай, Венгрия} с оговоркой «расширяется по факту». Точные страны производственных площадок препаратов Августа и других российских регистрантов известны частично — например, часть препаратов Августа фасуется на площадке в Венгрии (Bercencsurdo). Полная сверка — задача Phase 4 (миграция контента, работа с этикетками).

---

## 3. Блок «Другие категории» — счётчик Десикантов

**Что не так в v21:**
```js
{ name: "Десиканты", count: "5 препаратов", letter: "Д", href: "/catalog/desikanty/" }
```

**Что должно стать:**
```js
{ name: "Десиканты", count: "1 препарат", letter: "Д", href: "/catalog/desikanty/" }
```

**Основание:** по ПИ08 (уточнение 09.08.2026), в текущем ассортименте только один десикант — **Суховей, ВР** от АО Фирма «Август» (двойная регистрация 021-03(04)-9729-0, гербицид + десикант). Категория `/catalog/desikanty/` реализована как фильтрованное представление гербицидов по тегу `desikaciya`. Реальное число товаров, попадающих под этот фильтр, — 1.

**Также подправить связанное значение в GROUPS.tag:**

Было:
```js
{ key: "tag", title: "Тег применения", type: "single", extra: true,
  items: [["Все",44],["Только десиканты",5]] }
```

Стало:
```js
{ key: "tag", title: "Тег применения", type: "single", extra: true,
  items: [["Все",44],["Только десиканты",1]] }
```

**Форма склонения:** «1 препарат» (не «1 препаратов»). Проверить, что в мокапе рендерятся корректные окончания для 1 / 2–4 / 5+ (если разметка использует общий шаблон `X препаратов` — для 1 он должен подставлять «препарат»).

---

## 4. Что оставить без изменений

Все остальные элементы v21 соответствуют спеке. Не трогать:

- Полный набор фильтров (16 из 17 уже реализованы) — только добавляется «Цена».
- Компоновка сайдбара (4 развёрнутых + drawer через кнопку «Ещё фильтры»).
- Карточки товара — 6 бейджей, метка «Форма выпуска».
- Chip-строки, счётчик, «Только Август» — реализовано.
- Комментарии в коде про ПИ06 / ОВ18 / ПИ09 / ОВ20 — сохранить.
- CSS-переменная `--header-h`, sticky-позиционирование.
- Плейсхолдер серий (Торнадо, Балерина, Лазурит, Ластик, Дракон) — по комментарию про динамический рендер, финальный список в Phase 5.

---

## 5. Приёмка

При проверке v22 контролируется:

**Новый фильтр «Цена»:**
- [ ] В `const GROUPS` добавлен элемент с `type: "range"`, `key: "price"`, `title: "Цена, ₽"`, `extra: true`
- [ ] На UI фильтра — два поля ввода «От» / «До» + двухточечный слайдер
- [ ] Активный чип отображается только при сужении диапазона от границ
- [ ] Формат чипа: «Цена: 1 200 – 5 800 ₽» (или «Цена: от X ₽», «Цена: до Y ₽»)

**Фильтр «Страна производства»:**
- [ ] «Германия» удалена
- [ ] Добавлена «Венгрия» с плейсхолдер-числом 2
- [ ] Порядок значений: Россия, Китай, Венгрия
- [ ] Над строкой в коде — комментарий про Phase 4 и §5

**Счётчик Десикантов:**
- [ ] В блоке «Другие категории»: `count: "1 препарат"` (не «5 препаратов»)
- [ ] В `GROUPS.tag.items`: `["Только десиканты", 1]` (не `5`)
- [ ] Форма склонения корректная («1 препарат»)

---

## 6. Ссылки

- Приёмка v21: см. диалог PM-чата 09.08.2026 (13 из 14 пунктов чек-листа delta v20→v21)
- `catalog-ia-v1.md` v1.4 §5 (стартовый словарь «Страна производства»)
- `catalog-ia-v1.md` v1.4 §8 (полный перечень фильтров, п. 17 — Цена)
- `decisions-registry.md` — ПИ08 (Суховей = единственный десикант), ОВ20 закрыт (список производителей)
- `Восток-АгроХим_каталог_верификация_v2.xlsx` — источник фактических данных ассортимента (у Оли локально)
