# Snapshot: pre-field-journal — 18.08.2026

## Что это

Замороженное состояние трёх ключевых макетов **до** начала эксперимента с визуальным направлением «Полевой журнал» (ДМ36).

Источник — ZIP-архив `Восток-Агрохим_Hero_блок__39_.zip`, экспорт из Claude Design от 17.08.2026, полученный от Оли 18.08.2026. Это версии, которые на момент снимка ещё не были закоммичены в `phase-3-design/3.3-mockups/`.

## Зачем

Правило ДМ35: перед любым экспериментальным редизайном действующая принятая версия сохраняется в `snapshots/` под именованной датой. К ней можно откатиться в один клик, если эксперимент пойдёт не туда.

## Содержимое

| Файл | Ожидаемый размер | Что это |
|---|---:|---|
| `Hero.dc.html` | 65 927 байт | Главная страница, актуальная итерация после v19 |
| `catalog-listing-gerbicidy.dc.html` | 58 471 байт | Листинг категории «Гербициды», актуальная итерация после v22 |
| `Product_card_balerina.dc.html` | 69 658 байт | Пилотная карточка товара «Балерина, СЭ», актуальная итерация после v3 |

## Статус — файлы залиты не полностью

По состоянию на 18.08.2026 03:30 UTC в папке лежат **заглушки** (несколько сотен байт). Настоящие HTML-файлы должны быть загружены **вручную через веб-интерфейс GitHub** — API не даёт устойчивой передачи файлов такого размера (>50 KB) без риска усечения.

### Как залить

1. Распаковать `Восток-Агрохим_Hero_блок__39_.zip` локально.
2. В веб-интерфейсе GitHub открыть эту папку: [snapshots/pre-field-journal-2026-08-18/](.).
3. Нажать **Add file → Upload files** (или перетащить файлы прямо в окно браузера).
4. Загрузить три файла: `Hero.dc.html`, `catalog-listing-gerbicidy.dc.html`, `Product_card_balerina.dc.html`. GitHub автоматически заменит одноимённые заглушки.
5. Коммит-сообщение: `snapshot: заливаю настоящие HTML pre-field-journal`.
6. Удалить файл-остаток `catalog-listing-gerbicidy.dc.html.PLACEHOLDER` (кнопка «удалить» в правом верхнем углу файла).

### Проверка

После заливки размеры файлов должны совпадать с колонкой «Ожидаемый размер» в таблице выше — с точностью до 1 байта. Если размер отличается больше чем на 100 байт — файл усечён, залить заново.

## Куда положить те же файлы как рабочие

Помимо snapshot'а, эти же три файла — действующие «принятые» версии. Их место — `phase-3-design/3.3-mockups/`. Тем же способом (Upload files через веб) залей туда:
- `Hero.dc.html` — заменит текущий Hero v19 (63 401 байт)
- `Product_card_balerina.dc.html` — заменит текущую карточку (65 476 байт)
- `catalog-listing-gerbicidy.dc.html` — новый файл, его в mockups ещё нет

## Как откатиться

Если эксперимент «Полевой журнал» будет отменён:
1. Из этой папки скачать три HTML-файла.
2. Через веб-интерфейс GitHub загрузить их обратно в `phase-3-design/3.3-mockups/` поверх экспериментальных версий.
3. Зафиксировать в реестре решений отмену ДМ36 через ОВ или НР.
